# Aero API
A high-performance flight information management system

## Installation
1. Run `go mod init aero-api` to initialize the Go module
2. Run `go get` to install dependencies

## Execution
Run `go run src/main.go` to start the server